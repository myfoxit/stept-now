---
import Layout from '../layouts/Layout.astro'
import Wordmark from '../components/Wordmark.astro'
import Icon from '../components/Icon.astro'

const APP = 'https://app.stepped.ai'
const GITHUB = 'https://github.com/myfoxit/stept-now'
const DOCS = 'https://docs.stepped.ai'

const features = [
  {
    icon: 'inbox',
    title: 'One shared inbox',
    body: 'Every conversation in one place — assign, snooze, tag, note and resolve, with SLAs and realtime collaboration across your team.',
  },
  {
    icon: 'sparkles',
    title: 'Your own AI agent',
    body: 'An agent that answers from your knowledge base with citations, guardrails and human-in-the-loop approval gates — with a full tool-call trace for every run. Bring your own model — OpenAI, Anthropic, Google, Ollama or any OpenAI-compatible.',
  },
  {
    icon: 'book',
    title: 'Knowledge & RAG',
    body: 'Crawl your site, sync Confluence, Notion, Google Drive or Zendesk, or upload files. Hybrid retrieval powers both the agent and a public help center.',
  },
  {
    icon: 'messages',
    title: 'Every channel',
    body: 'Web widget, email, Slack, Telegram, WhatsApp, Messenger, Instagram, LINE, SMS and a public API — one conversation model behind them all.',
  },
  {
    icon: 'map',
    title: 'Product adoption built in',
    body: 'Record interactive product tours with a Chrome extension and play them in your app — plus checklists, surveys, banners and hotspots. Digital adoption, without a second vendor.',
  },
  {
    icon: 'route',
    title: 'Automation & reports',
    body: 'Rules, macros, campaigns, SLAs and webhooks — plus analytics on volume, resolution rate and CSAT.',
  },
]

const integrations = [
  'Gmail',
  'Microsoft 365',
  'Slack',
  'Notion',
  'Confluence',
  'Zendesk',
  'GitHub',
  'Google Drive',
  'Amazon SES',
  'Resend',
  'Postmark',
  'SendGrid',
  'Mailgun',
  'Telegram',
  'Claude (MCP)',
  'Cursor (MCP)',
]

const plans = [
  {
    name: 'Self-hosted',
    price: 'Free',
    unit: 'MIT · forever',
    highlight: false,
    cta: { label: 'Get the source', href: GITHUB },
    tagline: 'Run the whole thing yourself.',
    features: [
      'All channels, unlimited seats',
      'AI agent with your own model key',
      'Knowledge base, RAG & help center',
      'Product tours & automation',
      'Deploy with Docker Compose',
    ],
  },
  {
    name: 'Cloud',
    price: '$19',
    unit: 'per seat / month',
    highlight: true,
    cta: { label: 'Start free', href: `${APP}/signup` },
    tagline: 'We host and update it for you.',
    features: [
      'Everything in self-hosted',
      '500 AI resolutions / month included',
      'Managed Postgres & backups',
      'Automatic updates',
      'Email & chat support',
    ],
  },
  {
    name: 'Business',
    price: '$49',
    unit: 'per seat / month',
    highlight: false,
    cta: { label: 'Talk to us', href: 'mailto:hello@stepped.ai' },
    tagline: 'For teams with compliance needs.',
    features: [
      'Everything in Cloud',
      '2,000 AI resolutions / month',
      'Custom roles & audit log',
      'SLA management & working hours',
      'Priority support',
    ],
  },
]
const year = new Date().getFullYear()
---

<Layout>
  <!-- ===== nav ===== -->
  <header class="nav">
    <div class="container nav-inner">
      <a href="/" aria-label="Stept home"><Wordmark /></a>
      <nav class="nav-links" aria-label="Primary">
        <a href="#features">Product</a>
        <a href="#agent">AI agent</a>
        <a href="#integrations">Integrations</a>
        <a href="#pricing">Pricing</a>
        <a href={DOCS}>Docs</a>
        <a href={GITHUB}>GitHub</a>
      </nav>
      <div class="nav-cta">
        <a class="btn btn-ghost btn-sm nav-signin" href={`${APP}/login`}>Sign in</a>
        <a class="btn btn-primary btn-sm" href={`${APP}/signup`}>Start free</a>
        <details class="nav-menu">
          <summary aria-label="Open menu">
            <span class="nav-burger" aria-hidden="true"><i></i><i></i><i></i></span>
          </summary>
          <nav class="nav-menu-panel" aria-label="Mobile">
            <a href="#features">Product</a>
            <a href="#agent">AI agent</a>
            <a href="#integrations">Integrations</a>
            <a href="#pricing">Pricing</a>
            <a href={DOCS}>Docs</a>
            <a href={GITHUB}>GitHub</a>
            <a href={`${APP}/login`}>Sign in</a>
          </nav>
        </details>
      </div>
    </div>
  </header>

  <main>
    <!-- ===== hero ===== -->
    <section class="section hero">
      <div class="container hero-grid">
        <div class="hero-copy">
          <span class="eyebrow">
            <Icon name="sparkles" size={16} /> Open source · self-hostable
          </span>
          <h1>Customer support that resolves itself.</h1>
          <p class="lead">
            Stept is the open-source Intercom&nbsp;+&nbsp;Fin alternative: a shared inbox across
            every channel, an AI agent grounded in your own knowledge base, a help center, product
            tours and automation. Self-host it free — bring your own model key — or let us run it.
          </p>
          <div class="hero-actions">
            <a class="btn btn-primary" href={`${APP}/signup`}>
              Start free <Icon name="arrow" size={18} />
            </a>
            <a class="btn btn-ghost" href={GITHUB}>
              <Icon name="github" size={18} /> Star on GitHub
            </a>
          </div>
          <p class="hero-note">MIT-licensed · Postgres or SQLite · deploys with Docker Compose</p>
        </div>

        <!-- product mock: the Stept inbox, drawn in CSS (no real data) -->
        <div class="mock card" aria-hidden="true">
          <div class="mock-rail">
            <div class="mock-brand"><span class="mock-glyph">S</span></div>
            <span class="mock-ico on"></span>
            <span class="mock-ico"></span>
            <span class="mock-ico"></span>
            <span class="mock-ico"></span>
          </div>
          <div class="mock-list">
            <div class="mock-list-head"></div>
            <div class="mock-row active">
              <span class="mock-avatar"></span>
              <span class="mock-lines"><i></i><i class="short"></i></span>
            </div>
            <div class="mock-row">
              <span class="mock-avatar"></span>
              <span class="mock-lines"><i></i><i class="short"></i></span>
            </div>
            <div class="mock-row">
              <span class="mock-avatar"></span>
              <span class="mock-lines"><i></i><i class="short"></i></span>
            </div>
          </div>
          <div class="mock-thread">
            <div class="bubble in"></div>
            <div class="bubble in short"></div>
            <div class="ai">
              <div class="ai-head"><span class="ai-badge"><Icon name="sparkles" size={12} /> AI agent</span><span class="ai-conf"></span></div>
              <div class="ai-line"></div>
              <div class="ai-line"></div>
              <div class="ai-line short"></div>
              <div class="ai-cite"></div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ===== channels strip ===== -->
    <section class="strip">
      <div class="container">
        <p class="strip-label">One conversation model behind every channel</p>
        <div class="strip-items">
          {
            ['Web widget', 'Email', 'Slack', 'Telegram', 'WhatsApp', 'Messenger', 'Instagram', 'LINE', 'SMS', 'API'].map(
              (c) => <span class="chip">{c}</span>,
            )
          }
        </div>
      </div>
    </section>

    <!-- ===== features ===== -->
    <section class="section" id="features">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow"><Icon name="inbox" size={16} /> The platform</span>
          <h2>Everything a modern support team needs — in one place.</h2>
          <p class="lead">
            Intercom bundles the inbox, the AI agent, the help center and product tours, then
            charges for each. Stept ships all of it in one open-source platform.
          </p>
        </div>
        <div class="feature-grid">
          {
            features.map((f) => (
              <article class="card feature">
                <span class="feature-ico"><Icon name={f.icon} size={22} /></span>
                <h3>{f.title}</h3>
                <p>{f.body}</p>
              </article>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ===== the differentiator: agent with hands ===== -->
    <section class="section" id="agent">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow"><Icon name="bot" size={16} /> The differentiator</span>
          <h2>An agent with hands — not just answers.</h2>
          <p class="lead">
            Most support AI stops at text. Stept's agent can see the page your user is on, guide
            them through it, and act on it — and your whole workspace is scriptable from the
            outside.
          </p>
        </div>
        <div class="feature-grid">
          <article class="card feature">
            <span class="feature-ico"><Icon name="map" size={22} /></span>
            <h3>It shows, not tells</h3>
            <p>
              Instead of describing five steps, the agent finds the right product tour and plays
              it — or composes an on-the-spot walkthrough with coach-marks on your real UI.
            </p>
          </article>
          <article class="card feature">
            <span class="feature-ico"><Icon name="zap" size={22} /></span>
            <h3>It does things — with consent</h3>
            <p>
              With the user's explicit opt-in it clicks, fills and navigates for them. Risky tools
              pause for human approval, password fields are never touched, and every run keeps a
              full step-by-step trace.
            </p>
          </article>
          <article class="card feature">
            <span class="feature-ico"><Icon name="plug" size={22} /></span>
            <h3>MCP built in</h3>
            <p>
              Claude, Cursor or any MCP client can search your knowledge, read conversations,
              create docs and even run product tours in a real browser — through Stept's built-in
              MCP server.
            </p>
          </article>
        </div>
      </div>
    </section>

    <!-- ===== self-host / BYO band ===== -->
    <section class="section band-wrap">
      <div class="container">
        <div class="band card">
          <div class="band-copy">
            <span class="eyebrow"><Icon name="shield" size={16} /> Own your stack</span>
            <h2>Run your own Fin-class agent — free.</h2>
            <p class="lead">
              Most AI-support tools bill per resolution or lock the agent behind an enterprise
              plan. Stept ships the agent in the open-source core. Point it at your own model key
              and self-host the whole platform — inbox, knowledge base, tours and all — at no cost.
            </p>
            <pre class="install"><code>git clone https://github.com/myfoxit/stept-now
cd stept-now && make setup && make dev</code></pre>
            <div class="hero-actions">
              <a class="btn btn-primary" href={`${DOCS}/getting-started/self-hosting/`}>
                Read the self-hosting guide <Icon name="arrow" size={18} />
              </a>
            </div>
          </div>
          <ul class="band-list">
            <li><Icon name="check" size={18} /> Bring your own model — no per-resolution fee</li>
            <li><Icon name="check" size={18} /> Your data stays in your database</li>
            <li><Icon name="check" size={18} /> Postgres in prod, SQLite for a trial</li>
            <li><Icon name="check" size={18} /> No seat caps when you self-host</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- ===== integrations ===== -->
    <section class="section" id="integrations">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow"><Icon name="plug" size={16} /> Integrations</span>
          <h2>Connect the tools you already use.</h2>
          <p class="lead">
            OAuth into Gmail, Microsoft 365, Slack, Notion and Confluence. Connect Zendesk,
            Telegram and GitHub with a token. Send email through SES, Resend, Postmark, SendGrid,
            Mailgun or plain SMTP — and plug any MCP client straight into your workspace.
          </p>
        </div>
        <div class="integ-grid">
          {
            integrations.map((name) => (
              <div class="integ">
                <span class="integ-dot" />
                {name}
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ===== pricing ===== -->
    <section class="section" id="pricing">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow"><Icon name="zap" size={16} /> Pricing</span>
          <h2>Free to self-host. Fair when we host it.</h2>
          <p class="lead">
            The whole platform is open source. Pay only when you want us to run it — and undercut
            the incumbents while you do.
          </p>
          <p class="notice">
            <Icon name="sparkles" size={14} /> Early access: Stept Cloud is free right now — the
            plans below are the pricing that's coming.
          </p>
        </div>
        <div class="price-grid">
          {
            plans.map((p) => (
              <article class:list={['card plan', { featured: p.highlight }]}>
                {p.highlight && <span class="plan-badge">Most popular</span>}
                <h3 class="plan-name">{p.name}</h3>
                <div class="plan-price">
                  <span class="amount">{p.price}</span>
                  <span class="unit">{p.unit}</span>
                </div>
                <p class="plan-tagline">{p.tagline}</p>
                <a
                  class:list={['btn', p.highlight ? 'btn-primary' : 'btn-ghost', 'plan-cta']}
                  href={p.cta.href}
                >
                  {p.cta.label}
                </a>
                <ul class="plan-features">
                  {p.features.map((f) => (
                    <li>
                      <Icon name="check" size={16} /> {f}
                    </li>
                  ))}
                </ul>
              </article>
            ))
          }
        </div>
        <p class="price-foot">
          AI resolutions beyond your plan will bill at about <strong>$0.30–0.49 each</strong> —
          around half of Intercom Fin's $0.99. A 5-seat team pays about <strong>$95/mo</strong> on
          Stept Cloud; the same team on Intercom&nbsp;+&nbsp;Fin typically runs
          <strong>$1,000+/mo</strong>.
        </p>
      </div>
    </section>

    <!-- ===== FAQ ===== -->
    <section class="section" id="faq">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow"><Icon name="messages" size={16} /> FAQ</span>
          <h2>Questions, answered.</h2>
        </div>
        <div class="faq-list">
          <details class="card faq">
            <summary>Is it really open source?</summary>
            <p>
              Yes — the whole platform is MIT-licensed: inbox, AI agent, knowledge base, help
              center, tours, automation. Self-host everything with your own model key. Cloud is
              just us running the same code for you.
            </p>
          </details>
          <details class="card faq">
            <summary>Which AI models can I use?</summary>
            <p>
              OpenAI, Anthropic, Google, Ollama, or any OpenAI-compatible endpoint. Keys are
              stored per workspace, encrypted at rest — and there's no markup on tokens: you pay
              your provider directly.
            </p>
          </details>
          <details class="card faq">
            <summary>What counts as an "AI resolution"?</summary>
            <p>
              A conversation the agent resolves without a human stepping in. Self-hosted
              installs meter nothing. On Cloud, plan quotas apply once pricing turns on — during
              early access everything is free.
            </p>
          </details>
          <details class="card faq">
            <summary>Can the widget really control the page?</summary>
            <p>
              Only with the visitor's explicit per-conversation consent. Actions are capped per
              run, password fields are off-limits, navigation is same-origin by default, and
              sensitive tools can require a human approval before they execute.
            </p>
          </details>
          <details class="card faq">
            <summary>Where does my data live?</summary>
            <p>
              Self-hosted: in your own Postgres, full stop. On Stept Cloud your workspace runs on
              EU infrastructure (Hetzner), model keys are encrypted at rest, and your
              conversations are never used to train anything.
            </p>
          </details>
          <details class="card faq">
            <summary>Can I migrate from Intercom or Zendesk?</summary>
            <p>
              Import contacts via CSV, pull help-center content in with the website crawler or
              the Zendesk connector, and keep your existing email address via forwarding. A
              guided migration is on the roadmap — <a href="mailto:hello@stepped.ai">email us</a>
              and we'll help.
            </p>
          </details>
        </div>
      </div>
    </section>

    <!-- ===== final CTA ===== -->
    <section class="section">
      <div class="container">
        <div class="cta card">
          <h2>Own your support stack.</h2>
          <p class="lead">Deploy it yourself with Docker Compose, or start free on our cloud.</p>
          <div class="hero-actions">
            <a class="btn btn-primary" href={`${APP}/signup`}>Start free <Icon name="arrow" size={18} /></a>
            <a class="btn btn-ghost" href={`${DOCS}/getting-started/self-hosting/`}><Icon name="github" size={18} /> Self-host it</a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- ===== footer ===== -->
  <footer class="footer">
    <div class="container footer-inner">
      <div class="footer-brand">
        <Wordmark size="sm" />
        <p>The open-source Intercom + Fin alternative.</p>
      </div>
      <nav class="footer-col" aria-label="Product">
        <span class="footer-h">Product</span>
        <a href="#features">Features</a>
        <a href="#integrations">Integrations</a>
        <a href="#pricing">Pricing</a>
      </nav>
      <nav class="footer-col" aria-label="Resources">
        <span class="footer-h">Resources</span>
        <a href={DOCS}>Docs</a>
        <a href={GITHUB}>GitHub</a>
        <a href={`${APP}/login`}>Sign in</a>
        <a href={`${APP}/signup`}>Start free</a>
      </nav>
      <nav class="footer-col" aria-label="Project">
        <span class="footer-h">Project</span>
        <a href={GITHUB}>Source (MIT)</a>
        <a href={`${DOCS}/getting-started/self-hosting/`}>Self-host docs</a>
        <a href="mailto:hello@stepped.ai">Contact</a>
        <a href="/privacy">Privacy</a>
      </nav>
    </div>
    <div class="container footer-base">
      <span>© {year} Stept</span>
      <span>Open source · MIT</span>
    </div>
  </footer>
</Layout>

<style>
  /* ---- nav ---- */
  .nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in oklch, var(--background) 82%, transparent);
    backdrop-filter: saturate(1.4) blur(10px);
    border-bottom: 1px solid var(--border);
  }
  .nav-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 4rem;
    gap: 1rem;
  }
  .nav-links {
    display: none;
    gap: 1.75rem;
    font-size: 0.9375rem;
    color: var(--muted-foreground);
  }
  .nav-links a:hover {
    color: var(--foreground);
  }
  .nav-cta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  .nav-signin {
    display: none;
  }
  @media (min-width: 820px) {
    .nav-links {
      display: flex;
    }
    .nav-signin {
      display: inline-flex;
    }
  }

  /* ---- mobile menu (no-JS, details/summary) ---- */
  .nav-menu {
    position: relative;
  }
  .nav-menu summary {
    list-style: none;
    display: grid;
    place-items: center;
    width: 2.25rem;
    height: 2.25rem;
    border: 1px solid var(--border);
    border-radius: calc(var(--radius) - 2px);
    cursor: pointer;
    background: var(--card);
  }
  .nav-menu summary::-webkit-details-marker {
    display: none;
  }
  .nav-burger {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .nav-burger i {
    width: 1rem;
    height: 2px;
    border-radius: 2px;
    background: var(--foreground);
  }
  .nav-menu-panel {
    position: absolute;
    right: 0;
    top: calc(100% + 0.5rem);
    min-width: 12rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding: 0.5rem;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    box-shadow: var(--shadow-lg);
  }
  .nav-menu-panel a {
    padding: 0.5rem 0.75rem;
    border-radius: calc(var(--radius) - 2px);
    font-size: 0.9375rem;
  }
  .nav-menu-panel a:hover {
    background: var(--muted);
  }
  @media (min-width: 820px) {
    .nav-menu {
      display: none;
    }
  }

  /* ---- hero ---- */
  .hero {
    position: relative;
    overflow: hidden;
    padding-top: clamp(3rem, 6vw, 5rem);
  }
  .hero::before {
    content: '';
    position: absolute;
    inset: -30% 0 auto 0;
    height: 70%;
    background: radial-gradient(60% 60% at 70% 0%, var(--primary-soft), transparent 70%);
    pointer-events: none;
  }
  .hero-grid {
    position: relative;
    display: grid;
    gap: clamp(2.5rem, 5vw, 4rem);
    align-items: center;
  }
  @media (min-width: 960px) {
    .hero-grid {
      grid-template-columns: 1.05fr 1fr;
    }
  }
  .hero-copy h1 {
    font-size: clamp(2.4rem, 5.5vw, 3.6rem);
    margin-top: 1rem;
  }
  .hero-copy .lead {
    margin-top: 1.15rem;
    max-width: 46ch;
  }
  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.75rem;
  }
  .hero-note {
    margin-top: 1.1rem;
    font-size: 0.85rem;
    color: var(--muted-foreground);
  }

  /* ---- product mock ---- */
  .mock {
    display: grid;
    grid-template-columns: 3.25rem 1.1fr 1.4fr;
    height: 340px;
    overflow: hidden;
    box-shadow: var(--shadow-lg);
    padding: 0;
  }
  .mock-rail {
    background: var(--muted);
    border-right: 1px solid var(--border);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    padding: 0.85rem 0;
  }
  .mock-brand {
    margin-bottom: 0.4rem;
  }
  .mock-glyph {
    display: grid;
    place-items: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: calc(var(--radius) - 2px);
    background: var(--primary);
    color: var(--primary-foreground);
    font-weight: 700;
    font-size: 0.85rem;
  }
  .mock-ico {
    width: 1.35rem;
    height: 1.35rem;
    border-radius: 6px;
    background: color-mix(in oklch, var(--muted-foreground) 22%, transparent);
  }
  .mock-ico.on {
    background: var(--primary);
    opacity: 0.85;
  }
  .mock-list {
    border-right: 1px solid var(--border);
    padding: 0.85rem;
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
  }
  .mock-list-head {
    height: 0.8rem;
    width: 55%;
    border-radius: 5px;
    background: color-mix(in oklch, var(--muted-foreground) 28%, transparent);
  }
  .mock-row {
    display: flex;
    gap: 0.55rem;
    align-items: center;
    padding: 0.5rem;
    border-radius: 8px;
  }
  .mock-row.active {
    background: var(--primary-soft);
  }
  .mock-avatar {
    width: 1.6rem;
    height: 1.6rem;
    border-radius: 50%;
    background: color-mix(in oklch, var(--muted-foreground) 30%, transparent);
    flex: none;
  }
  .mock-lines {
    display: flex;
    flex-direction: column;
    gap: 0.32rem;
    flex: 1;
  }
  .mock-lines i {
    height: 0.5rem;
    border-radius: 4px;
    background: color-mix(in oklch, var(--muted-foreground) 24%, transparent);
  }
  .mock-lines i.short {
    width: 60%;
  }
  .mock-thread {
    padding: 0.95rem;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }
  .bubble {
    height: 1.4rem;
    width: 70%;
    border-radius: 10px;
    background: var(--muted);
  }
  .bubble.short {
    width: 45%;
  }
  .ai {
    margin-top: auto;
    border: 1px solid var(--primary-ring);
    background: var(--primary-soft);
    border-radius: 12px;
    padding: 0.7rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }
  .ai-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .ai-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    font-size: 0.7rem;
    font-weight: 700;
    color: var(--primary);
    background: color-mix(in oklch, var(--primary) 14%, transparent);
    padding: 0.15rem 0.45rem;
    border-radius: 999px;
  }
  .ai-conf {
    width: 3rem;
    height: 0.4rem;
    border-radius: 4px;
    background: color-mix(in oklch, var(--primary) 40%, transparent);
  }
  .ai-line {
    height: 0.5rem;
    border-radius: 4px;
    background: color-mix(in oklch, var(--primary) 30%, transparent);
  }
  .ai-line.short {
    width: 55%;
  }
  .ai-cite {
    margin-top: 0.15rem;
    height: 0.9rem;
    width: 40%;
    border-radius: 5px;
    background: color-mix(in oklch, var(--primary) 18%, transparent);
  }

  /* ---- channels strip ---- */
  .strip {
    border-block: 1px solid var(--border);
    background: var(--muted);
    padding-block: 1.75rem;
  }
  .strip-label {
    text-align: center;
    font-size: 0.8125rem;
    color: var(--muted-foreground);
    margin-bottom: 0.9rem;
  }
  .strip-items {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.5rem;
  }
  .chip {
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--foreground);
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 0.35rem 0.8rem;
  }

  /* ---- section head ---- */
  .section-head {
    max-width: 640px;
    margin-bottom: 2.5rem;
  }
  .section-head h2 {
    font-size: clamp(1.8rem, 3.5vw, 2.4rem);
    margin-top: 0.75rem;
  }
  .section-head .lead {
    margin-top: 0.9rem;
    max-width: 56ch;
  }

  /* ---- features ---- */
  .feature-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(1, 1fr);
  }
  @media (min-width: 620px) {
    .feature-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (min-width: 960px) {
    .feature-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  .feature {
    padding: 1.5rem;
  }
  .feature-ico {
    display: grid;
    place-items: center;
    width: 2.75rem;
    height: 2.75rem;
    border-radius: var(--radius);
    background: var(--primary-soft);
    color: var(--primary);
    margin-bottom: 1rem;
  }
  .feature h3 {
    font-size: 1.1rem;
  }
  .feature p {
    margin-top: 0.5rem;
    color: var(--muted-foreground);
    font-size: 0.95rem;
  }

  /* ---- self-host band ---- */
  .band {
    padding: clamp(1.75rem, 4vw, 3rem);
    display: grid;
    gap: 2rem;
    background:
      radial-gradient(80% 120% at 100% 0%, var(--primary-soft), transparent 60%), var(--card);
  }
  @media (min-width: 900px) {
    .band {
      grid-template-columns: 1.3fr 1fr;
      align-items: center;
    }
  }
  .band-copy h2 {
    font-size: clamp(1.7rem, 3.4vw, 2.3rem);
    margin-top: 0.75rem;
  }
  .band-copy .lead {
    margin-top: 0.9rem;
  }
  .band-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.85rem;
  }
  .band-list li {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    font-weight: 500;
  }
  .band-list :global(svg) {
    color: var(--primary);
    flex: none;
  }
  .install {
    margin-top: 1.25rem;
    padding: 0.9rem 1.1rem;
    background: color-mix(in oklch, var(--foreground) 5%, var(--background));
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow-x: auto;
  }
  .install code {
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.85rem;
    color: var(--foreground);
    white-space: pre;
  }

  /* ---- pricing notice ---- */
  .notice {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    margin-top: 1rem;
    padding: 0.45rem 0.9rem;
    border: 1px solid var(--primary-ring);
    background: var(--primary-soft);
    color: var(--primary);
    border-radius: 999px;
    font-size: 0.875rem;
    font-weight: 500;
  }

  /* ---- FAQ ---- */
  .faq-list {
    display: grid;
    gap: 0.75rem;
    max-width: 760px;
  }
  .faq {
    padding: 0;
  }
  .faq summary {
    list-style: none;
    cursor: pointer;
    padding: 1.1rem 1.25rem;
    font-weight: 600;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
  }
  .faq summary::-webkit-details-marker {
    display: none;
  }
  .faq summary::after {
    content: '+';
    color: var(--muted-foreground);
    font-size: 1.2rem;
    line-height: 1;
    flex: none;
  }
  .faq[open] summary::after {
    content: '–';
  }
  .faq p {
    padding: 0 1.25rem 1.15rem;
    color: var(--muted-foreground);
    font-size: 0.95rem;
    max-width: 70ch;
  }
  .faq a {
    color: var(--primary);
    text-decoration: underline;
  }

  /* ---- integrations ---- */
  .integ-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
  }
  @media (min-width: 620px) {
    .integ-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  @media (min-width: 900px) {
    .integ-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  .integ {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.9rem 1rem;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--card);
    font-weight: 500;
    font-size: 0.95rem;
  }
  .integ-dot {
    width: 0.6rem;
    height: 0.6rem;
    border-radius: 50%;
    background: var(--primary);
    flex: none;
  }

  /* ---- pricing ---- */
  .price-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: 1fr;
  }
  @media (min-width: 900px) {
    .price-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  .plan {
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    position: relative;
  }
  .plan.featured {
    border-color: var(--primary);
    box-shadow: 0 0 0 1px var(--primary), var(--shadow-md);
  }
  .plan-badge {
    position: absolute;
    top: -0.7rem;
    right: 1.25rem;
    background: var(--primary);
    color: var(--primary-foreground);
    font-size: 0.7rem;
    font-weight: 700;
    padding: 0.2rem 0.6rem;
    border-radius: 999px;
  }
  .plan-name {
    font-size: 1.05rem;
  }
  .plan-price {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin-top: 0.5rem;
  }
  .plan-price .amount {
    font-size: 2.25rem;
    font-weight: 700;
    letter-spacing: -0.03em;
  }
  .plan-price .unit {
    color: var(--muted-foreground);
    font-size: 0.85rem;
  }
  .plan-tagline {
    color: var(--muted-foreground);
    font-size: 0.9rem;
    margin-top: 0.6rem;
  }
  .plan-cta {
    width: 100%;
    margin-top: 1.25rem;
  }
  .plan-features {
    list-style: none;
    margin: 1.5rem 0 0;
    padding: 0;
    display: grid;
    gap: 0.7rem;
  }
  .plan-features li {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    font-size: 0.9rem;
    color: var(--card-foreground);
  }
  .plan-features :global(svg) {
    color: var(--primary);
    flex: none;
  }
  .price-foot {
    text-align: center;
    color: var(--muted-foreground);
    font-size: 0.9rem;
    max-width: 60ch;
    margin: 2rem auto 0;
  }
  .price-foot strong {
    color: var(--foreground);
  }

  /* ---- final CTA ---- */
  .cta {
    text-align: center;
    padding: clamp(2.5rem, 6vw, 4rem);
    background:
      radial-gradient(80% 140% at 50% 0%, var(--primary-soft), transparent 65%), var(--card);
  }
  .cta h2 {
    font-size: clamp(1.9rem, 4vw, 2.6rem);
  }
  .cta .lead {
    margin: 0.9rem auto 0;
  }
  .cta .hero-actions {
    justify-content: center;
  }

  /* ---- footer ---- */
  .footer {
    border-top: 1px solid var(--border);
    padding-block: 3rem 2rem;
    margin-top: 2rem;
  }
  .footer-inner {
    display: grid;
    gap: 2rem;
    grid-template-columns: 1fr;
  }
  @media (min-width: 720px) {
    .footer-inner {
      grid-template-columns: 2fr 1fr 1fr 1fr;
    }
  }
  .footer-brand p {
    color: var(--muted-foreground);
    font-size: 0.9rem;
    margin-top: 0.75rem;
    max-width: 30ch;
  }
  .footer-col {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    font-size: 0.9rem;
    color: var(--muted-foreground);
  }
  .footer-col a:hover {
    color: var(--foreground);
  }
  .footer-h {
    color: var(--foreground);
    font-weight: 600;
    margin-bottom: 0.15rem;
  }
  .footer-base {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 2.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border);
    color: var(--muted-foreground);
    font-size: 0.85rem;
  }
</style>
