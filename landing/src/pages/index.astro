---
import Layout from '../layouts/Layout.astro'
import Wordmark from '../components/Wordmark.astro'
import Icon from '../components/Icon.astro'
import BrandIcon from '../components/BrandIcon.astro'
import Shot from '../components/Shot.astro'

const APP = 'https://app.stepped.ai'
const GITHUB = 'https://github.com/myfoxit/stept-now'
const DOCS = 'https://docs.stepped.ai'

const channels = [
  { brand: 'gmail', label: 'Email' },
  { brand: 'slack', label: 'Slack' },
  { brand: 'telegram', label: 'Telegram' },
  { brand: 'whatsapp', label: 'WhatsApp' },
  { brand: 'messenger', label: 'Messenger' },
  { brand: 'instagram', label: 'Instagram' },
  { brand: 'line', label: 'LINE' },
] as const

const sources = [
  { brand: 'notion', label: 'Notion' },
  { brand: 'confluence', label: 'Confluence' },
  { brand: 'drive', label: 'Google Drive' },
  { brand: 'zendesk', label: 'Zendesk' },
  { brand: 'github', label: 'GitHub' },
  { brand: 'microsoft', label: 'Microsoft 365' },
] as const

const models = [
  { brand: 'anthropic', label: 'Anthropic' },
  { brand: 'google', label: 'Google' },
  { brand: 'ollama', label: 'Ollama' },
  { brand: 'claude', label: 'Claude (MCP)' },
  { brand: 'cursor', label: 'Cursor (MCP)' },
] as const

const features = [
  {
    icon: 'inbox',
    title: 'One shared inbox',
    body: 'Assign, snooze, tag, note and resolve together — with SLAs, saved views, macros and realtime collaboration across the team.',
  },
  {
    icon: 'book',
    title: 'Knowledge that stays current',
    body: 'Crawl your site, upload files, or sync Notion, Confluence, Drive, Zendesk and GitHub. Hybrid retrieval feeds the agent, the reply copilot and your help center.',
  },
  {
    icon: 'route',
    title: 'Automation & reporting',
    body: 'Rules, macros, campaigns, SLAs and webhooks — plus analytics on volume, resolution rate, CSAT and how often the AI deflects a conversation.',
  },
  {
    icon: 'shield',
    title: 'Built for real teams',
    body: 'Multi-workspace tenancy, RBAC with custom roles, audit log, scoped API keys, encrypted provider secrets and a hardened deploy.',
  },
]

const plans = [
  {
    name: 'Self-hosted',
    price: 'Free',
    unit: 'MIT · forever',
    highlight: false,
    cta: { label: 'Read the guide', href: `${DOCS}/getting-started/self-hosting/` },
    tagline: 'Run the whole platform yourself.',
    features: [
      'Every channel, unlimited seats',
      'AI agent with your own model key',
      'Knowledge base, RAG & help center',
      'Product tours, checklists & surveys',
      'Docker Compose, Postgres or SQLite',
    ],
  },
  {
    name: 'Cloud',
    price: '$19',
    unit: 'per seat / month',
    highlight: true,
    cta: { label: 'Start free', href: `${APP}/signup` },
    tagline: 'We host, update and back it up.',
    features: [
      'Everything in self-hosted',
      '500 AI resolutions / month',
      'Managed Postgres & nightly backups',
      'Automatic updates',
      'Email support',
    ],
  },
  {
    name: 'Business',
    price: '$49',
    unit: 'per seat / month',
    highlight: false,
    cta: { label: 'Talk to us', href: 'mailto:hello@stepped.ai' },
    tagline: 'For teams with compliance needs.',
    features: [
      'Everything in Cloud',
      '2,000 AI resolutions / month',
      'Custom roles & audit log',
      'SLA management & working hours',
      'Priority support',
    ],
  },
]

const faqs = [
  {
    q: 'Is it really open source?',
    a: 'Yes — the whole platform is MIT-licensed: inbox, AI agent, knowledge base, help center, tours, automation. Self-host everything with your own model key. Cloud is just us running the same code for you.',
  },
  {
    q: 'Which AI models can I use?',
    a: 'OpenAI, Anthropic, Google, Ollama, or any OpenAI-compatible endpoint. Keys are stored per workspace and encrypted at rest, and there is no markup on tokens — you pay your provider directly. A deterministic mock provider ships in the box, so the entire AI stack runs offline with zero API keys.',
  },
  {
    q: 'What counts as an “AI resolution”?',
    a: 'A conversation the agent resolves without a human stepping in. Self-hosted installs meter nothing at all. On Cloud, plan quotas apply once pricing turns on — during early access everything is free.',
  },
  {
    q: 'Can the agent really act on the page?',
    a: 'Only with the visitor’s explicit per-conversation consent. Actions are capped per run, password fields are never touched, navigation is same-origin by default, and any tool can be set to require a human approval before it executes.',
  },
  {
    q: 'Where does my data live?',
    a: 'Self-hosted: in your own Postgres, full stop. On Stept Cloud your workspace runs on EU infrastructure, model keys are encrypted at rest, and your conversations are never used to train anything.',
  },
  {
    q: 'Can I migrate from Intercom or Zendesk?',
    a: 'Import contacts by CSV, pull help-center content in with the website crawler or the Zendesk connector, and keep your address by forwarding mail to your Stept inbox. Email us and we will help.',
  },
]

const year = new Date().getFullYear()
---

<Layout>
  <a href="#main" class="skip">Skip to content</a>

  <!-- ===== nav ===== -->
  <header class="nav">
    <div class="container nav-inner">
      <a href="/" aria-label="Stept home"><Wordmark /></a>
      <nav class="nav-links" aria-label="Primary">
        <a href="#product">Product</a>
        <a href="#agent">AI agent</a>
        <a href="#integrations">Integrations</a>
        <a href="#pricing">Pricing</a>
        <a href={DOCS}>Docs</a>
      </nav>
      <div class="nav-cta">
        <a class="nav-icon" href={GITHUB} aria-label="Stept on GitHub">
          <BrandIcon name="github" size={18} />
        </a>
        <a class="btn btn-ghost btn-sm nav-signin" href={`${APP}/login`}>Sign in</a>
        <a class="btn btn-primary btn-sm" href={`${APP}/signup`}>Start free</a>
        <details class="nav-menu">
          <summary aria-label="Open menu">
            <span class="nav-burger" aria-hidden="true"><i></i><i></i><i></i></span>
          </summary>
          <nav class="nav-menu-panel" aria-label="Mobile">
            <a href="#product">Product</a>
            <a href="#agent">AI agent</a>
            <a href="#integrations">Integrations</a>
            <a href="#pricing">Pricing</a>
            <a href={DOCS}>Docs</a>
            <a href={GITHUB}>GitHub</a>
            <a href={`${APP}/login`}>Sign in</a>
          </nav>
        </details>
      </div>
    </div>
  </header>

  <main id="main">
    <!-- ===== hero ===== -->
    <section class="hero">
      <div class="container">
        <div class="hero-copy">
          <a class="badge" href="#agent">
            <span class="badge-dot"></span>
            The agent doesn’t just answer — it shows and does
            <Icon name="arrow" size={13} />
          </a>
          <h1>Customer support that resolves itself.</h1>
          <p class="lead hero-lead">
            Stept is the open-source Intercom&nbsp;+&nbsp;Fin alternative: a shared inbox across
            every channel, an AI agent grounded in your own knowledge base, a help center,
            product tours and automation. Self-host it free — bring your own model key — or let
            us run it.
          </p>
          <div class="hero-actions">
            <a class="btn btn-primary" href={`${APP}/signup`}>
              Start free <Icon name="arrow" size={17} />
            </a>
            <a class="btn btn-ghost" href={DOCS}>Read the docs</a>
          </div>
          <p class="hero-note">
            MIT-licensed · runs offline with zero API keys · Postgres or SQLite
          </p>
        </div>

        <div class="hero-shot">
          <Shot
            src="/shots/inbox.png"
            alt="The Stept shared inbox with a customer conversation open, showing the thread, contact panel and reply composer"
            url="app.stepped.ai/inbox"
            priority
          />
        </div>
      </div>
    </section>

    <!-- ===== logo strip ===== -->
    <section class="strip">
      <div class="container">
        <p class="strip-label">Connects to the tools your customers and team already use</p>
        <div class="strip-logos">
          {
            [...channels, ...sources, ...models].map((item) => (
              <span class="logo" title={item.label}>
                <BrandIcon name={item.brand} size={22} />
              </span>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ===== the differentiator ===== -->
    <section class="section" id="agent">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow eyebrow-accent">
            <Icon name="sparkles" size={14} /> The differentiator
          </span>
          <h2>An agent with hands.</h2>
          <p class="lead">
            Most support AI stops at text. Stept’s agent reads your docs, guides people through
            your actual UI, and — with consent — acts on the page. Every run leaves a full
            step-by-step trace you can audit.
          </p>
        </div>

        <div class="split">
          <div class="split-copy">
            <div class="stack">
              <article class="point">
                <span class="point-ico"><Icon name="map" size={18} /></span>
                <div>
                  <h3>It shows, instead of telling</h3>
                  <p>
                    Rather than describing five steps, the agent finds the right product tour and
                    plays it — or composes a walkthrough on the spot, with coach-marks on your
                    real interface.
                  </p>
                </div>
              </article>
              <article class="point">
                <span class="point-ico"><Icon name="zap" size={18} /></span>
                <div>
                  <h3>It acts — with consent and a brake</h3>
                  <p>
                    With the visitor’s explicit opt-in it clicks, fills and navigates for them.
                    Sensitive tools pause for human approval, password fields are off-limits, and
                    actions are capped per run.
                  </p>
                </div>
              </article>
              <article class="point">
                <span class="point-ico"><Icon name="plug" size={18} /></span>
                <div>
                  <h3>Your workspace is an MCP server</h3>
                  <p>
                    Claude, Cursor or any MCP client can search your knowledge, read
                    conversations, write docs — even drive a real browser through the recorder
                    extension.
                  </p>
                </div>
              </article>
            </div>
          </div>
          <div class="split-shot">
            <Shot
              src="/shots/agent-trace.png"
              alt="An agent run in Stept showing the full execution trace: LLM call, tool call to search_knowledge, tool result, and the final reply, with token counts and timings"
              url="app.stepped.ai/ai/runs"
            />
          </div>
        </div>
      </div>
    </section>

    <!-- ===== product ===== -->
    <section class="section section-alt" id="product">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow">The platform</span>
          <h2>Everything a support team needs — in one place.</h2>
          <p class="lead">
            Intercom bundles the inbox, the AI agent, the help center and product tours, then
            charges for each. Stept ships all of it in one open-source platform.
          </p>
        </div>

        <div class="bento">
          <div class="bento-wide card">
            <div class="bento-copy">
              <h3>Configure the agent, then watch what it does</h3>
              <p>
                Pick the model, write the persona, and set every tool to auto, approval-required
                or off. Test it in a sandbox that shows the live reasoning trace before anything
                reaches a customer.
              </p>
            </div>
            <div class="bento-shot">
              <Shot
                src="/shots/agent-builder.png"
                alt="The Stept AI agent builder: identity, model, temperature and system prompt, with a test sandbox on the right"
                url="app.stepped.ai/ai/agents"
              />
            </div>
          </div>

          <div class="bento-half card">
            <div class="bento-copy">
              <h3>Knowledge that keeps itself fresh</h3>
              <p>
                Crawl, upload or sync. Sources re-index on a schedule and prune pages that
                disappear.
              </p>
            </div>
            <div class="bento-shot bento-shot-crop">
              <Shot
                src="/shots/knowledge.png"
                alt="The Stept knowledge base listing sources and indexed documents"
                url="app.stepped.ai/knowledge"
              />
            </div>
          </div>

          <div class="bento-half card">
            <div class="bento-copy">
              <h3>Tours, recorded by clicking</h3>
              <p>
                Record a flow in your own app with the Chrome extension; steps self-heal when
                your markup moves.
              </p>
            </div>
            <div class="bento-shot bento-shot-crop">
              <Shot
                src="/shots/tours.png"
                alt="Product tours in Stept, showing published tours with step counts and completion analytics"
                url="app.stepped.ai/tours"
              />
            </div>
          </div>
        </div>

        <div class="feature-grid">
          {
            features.map((f) => (
              <article class="feature">
                <span class="feature-ico">
                  <Icon name={f.icon} size={19} />
                </span>
                <h3>{f.title}</h3>
                <p>{f.body}</p>
              </article>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ===== self-host band ===== -->
    <section class="section">
      <div class="container">
        <div class="band">
          <div class="band-copy">
            <span class="eyebrow">Own your stack</span>
            <h2>Run your own Fin-class agent — free.</h2>
            <p class="lead">
              Most AI-support tools bill per resolution or lock the agent behind an enterprise
              plan. Stept ships the agent in the open-source core. Point it at your own model key
              and self-host the whole platform at no cost.
            </p>
            <pre class="install"><code><span class="c">$</span> git clone https://github.com/myfoxit/stept-now stept
<span class="c">$</span> cd stept && make setup && make dev</code></pre>
            <div class="hero-actions">
              <a class="btn btn-ghost" href={`${DOCS}/getting-started/self-hosting/`}>
                Self-hosting guide <Icon name="arrow" size={17} />
              </a>
            </div>
          </div>
          <ul class="band-list">
            <li><Icon name="check" size={17} /> Bring your own model — no per-resolution fee</li>
            <li><Icon name="check" size={17} /> Your data stays in your own database</li>
            <li><Icon name="check" size={17} /> Runs offline: mock AI provider, zero keys</li>
            <li><Icon name="check" size={17} /> No seat caps when you self-host</li>
            <li><Icon name="check" size={17} /> ~1,900 tests, green on every push</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- ===== integrations ===== -->
    <section class="section section-alt" id="integrations">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow">Integrations</span>
          <h2>Connect the tools you already use.</h2>
        </div>
        <div class="integ-cols">
          <div class="integ-col">
            <h3 class="integ-h">Channels</h3>
            <ul>
              {
                channels.map((c) => (
                  <li>
                    <span class="integ-logo">
                      <BrandIcon name={c.brand} size={18} color />
                    </span>
                    {c.label}
                  </li>
                ))
              }
              <li><span class="integ-logo integ-generic"><Icon name="messages" size={16} /></span>Web widget & API</li>
            </ul>
          </div>
          <div class="integ-col">
            <h3 class="integ-h">Knowledge</h3>
            <ul>
              {
                sources.map((s) => (
                  <li>
                    <span class="integ-logo">
                      <BrandIcon name={s.brand} size={18} color />
                    </span>
                    {s.label}
                  </li>
                ))
              }
              <li><span class="integ-logo integ-generic"><Icon name="book" size={16} /></span>Website crawl & files</li>
            </ul>
          </div>
          <div class="integ-col">
            <h3 class="integ-h">Models & clients</h3>
            <ul>
              {
                models.map((m) => (
                  <li>
                    <span class="integ-logo">
                      <BrandIcon name={m.brand} size={18} color />
                    </span>
                    {m.label}
                  </li>
                ))
              }
              <li><span class="integ-logo integ-generic"><Icon name="sparkles" size={16} /></span>Any OpenAI-compatible</li>
            </ul>
          </div>
          <div class="integ-col">
            <h3 class="integ-h">Email delivery</h3>
            <ul>
              <li><span class="integ-logo"><BrandIcon name="amazon" size={18} color /></span>Amazon SES</li>
              <li><span class="integ-logo"><BrandIcon name="resend" size={18} color /></span>Resend</li>
              <li><span class="integ-logo"><BrandIcon name="mailgun" size={18} color /></span>Mailgun</li>
              <li><span class="integ-logo integ-generic"><Icon name="inbox" size={16} /></span>Postmark</li>
              <li><span class="integ-logo integ-generic"><Icon name="inbox" size={16} /></span>SendGrid</li>
              <li><span class="integ-logo integ-generic"><Icon name="inbox" size={16} /></span>Plain SMTP</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- ===== pricing ===== -->
    <section class="section" id="pricing">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow">Pricing</span>
          <h2>Free to self-host. Fair when we host it.</h2>
          <p class="notice">
            <span class="badge-dot"></span> Early access: Stept Cloud is free right now — below is
            the pricing that’s coming.
          </p>
        </div>
        <div class="price-grid">
          {
            plans.map((p) => (
              <article class:list={['plan card', { featured: p.highlight }]}>
                {p.highlight && <span class="plan-badge">Most popular</span>}
                <h3 class="plan-name">{p.name}</h3>
                <div class="plan-price">
                  <span class="amount">{p.price}</span>
                  <span class="unit">{p.unit}</span>
                </div>
                <p class="plan-tagline">{p.tagline}</p>
                <a
                  class:list={['btn', p.highlight ? 'btn-primary' : 'btn-ghost', 'plan-cta']}
                  href={p.cta.href}
                >
                  {p.cta.label}
                </a>
                <ul class="plan-features">
                  {p.features.map((f) => (
                    <li>
                      <Icon name="check" size={15} /> {f}
                    </li>
                  ))}
                </ul>
              </article>
            ))
          }
        </div>
        <p class="price-foot">
          AI resolutions beyond your plan will bill at about <strong>$0.30–0.49 each</strong> —
          around half of Intercom Fin’s $0.99. A 5-seat team pays about <strong>$95/mo</strong> on
          Stept Cloud; the same team on Intercom&nbsp;+&nbsp;Fin typically runs
          <strong>$1,000+/mo</strong>.
        </p>
      </div>
    </section>

    <!-- ===== FAQ ===== -->
    <section class="section section-alt" id="faq">
      <div class="container">
        <div class="section-head">
          <span class="eyebrow">FAQ</span>
          <h2>Questions, answered.</h2>
        </div>
        <div class="faq-list">
          {
            faqs.map((f) => (
              <details class="faq">
                <summary>{f.q}</summary>
                <p>{f.a}</p>
              </details>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ===== final CTA ===== -->
    <section class="section">
      <div class="container">
        <div class="cta">
          <h2>Own your support stack.</h2>
          <p class="lead">Start free on our cloud, or deploy it yourself with Docker Compose.</p>
          <div class="hero-actions">
            <a class="btn btn-primary" href={`${APP}/signup`}>
              Start free <Icon name="arrow" size={17} />
            </a>
            <a class="btn btn-ghost" href={GITHUB}>
              <BrandIcon name="github" size={17} /> Star on GitHub
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- ===== footer ===== -->
  <footer class="footer">
    <div class="container footer-inner">
      <div class="footer-brand">
        <Wordmark size="sm" />
        <p>The open-source Intercom + Fin alternative.</p>
      </div>
      <nav class="footer-col" aria-label="Product">
        <span class="footer-h">Product</span>
        <a href="#product">Features</a>
        <a href="#agent">AI agent</a>
        <a href="#integrations">Integrations</a>
        <a href="#pricing">Pricing</a>
      </nav>
      <nav class="footer-col" aria-label="Resources">
        <span class="footer-h">Resources</span>
        <a href={DOCS}>Documentation</a>
        <a href={`${DOCS}/getting-started/self-hosting/`}>Self-hosting</a>
        <a href={`${DOCS}/integrations/mcp/`}>MCP server</a>
        <a href={GITHUB}>GitHub</a>
      </nav>
      <nav class="footer-col" aria-label="Company">
        <span class="footer-h">Company</span>
        <a href={`${APP}/login`}>Sign in</a>
        <a href="mailto:hello@stepped.ai">Contact</a>
        <a href="/privacy">Privacy</a>
      </nav>
    </div>
    <div class="container footer-base">
      <span>© {year} Stept</span>
      <span>Open source · MIT</span>
    </div>
  </footer>
</Layout>

<style>
  .skip {
    position: absolute;
    left: -9999px;
    top: 0;
    background: var(--action);
    color: var(--action-foreground);
    padding: 0.6rem 1rem;
    border-radius: var(--radius);
    z-index: 100;
  }
  .skip:focus {
    left: 1rem;
    top: 1rem;
  }

  /* ---- nav ---- */
  .nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in srgb, var(--background) 80%, transparent);
    backdrop-filter: saturate(1.6) blur(12px);
    border-bottom: 1px solid var(--border);
  }
  .nav-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 3.75rem;
    gap: 1rem;
  }
  .nav-links {
    display: none;
    gap: 1.5rem;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--muted-foreground);
  }
  .nav-links a:hover {
    color: var(--foreground);
  }
  .nav-cta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  .nav-icon {
    display: none;
    place-items: center;
    width: 2.25rem;
    height: 2.25rem;
    border-radius: var(--radius);
    color: var(--muted-foreground);
  }
  .nav-icon:hover {
    color: var(--foreground);
    background: var(--muted);
  }
  .nav-signin {
    display: none;
  }
  @media (min-width: 900px) {
    .nav-links,
    .nav-signin {
      display: flex;
    }
    .nav-icon {
      display: grid;
    }
  }

  /* ---- mobile menu (no JS) ---- */
  .nav-menu {
    position: relative;
  }
  .nav-menu summary {
    list-style: none;
    display: grid;
    place-items: center;
    width: 2.25rem;
    height: 2.25rem;
    border: 1px solid var(--border-strong);
    border-radius: var(--radius);
    cursor: pointer;
    background: var(--background);
  }
  .nav-menu summary::-webkit-details-marker {
    display: none;
  }
  .nav-burger {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .nav-burger i {
    width: 0.9rem;
    height: 1.5px;
    border-radius: 2px;
    background: var(--foreground);
  }
  .nav-menu-panel {
    position: absolute;
    right: 0;
    top: calc(100% + 0.5rem);
    min-width: 12rem;
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
    padding: 0.5rem;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    box-shadow: var(--shadow-lg);
  }
  .nav-menu-panel a {
    padding: 0.5rem 0.75rem;
    border-radius: calc(var(--radius) - 2px);
    font-size: 0.9rem;
  }
  .nav-menu-panel a:hover {
    background: var(--muted);
  }
  @media (min-width: 900px) {
    .nav-menu {
      display: none;
    }
  }

  /* ---- hero ---- */
  .hero {
    position: relative;
    padding-block: clamp(3.5rem, 8vw, 6rem) clamp(2.5rem, 5vw, 4rem);
    overflow: hidden;
    text-align: center;
  }
  .hero::before {
    content: '';
    position: absolute;
    inset: -40% 20% auto 20%;
    height: 60%;
    background: radial-gradient(50% 50% at 50% 50%, var(--primary-soft), transparent 70%);
    pointer-events: none;
  }
  .hero-copy {
    position: relative;
    max-width: 46rem;
    margin-inline: auto;
  }
  .badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--muted-foreground);
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 0.3rem 0.85rem 0.3rem 0.7rem;
    transition: border-color 0.15s ease;
  }
  .badge:hover {
    border-color: var(--border-strong);
    color: var(--foreground);
  }
  .badge-dot {
    width: 0.4rem;
    height: 0.4rem;
    border-radius: 50%;
    background: var(--primary);
    flex: none;
  }
  .hero h1 {
    font-size: clamp(2.5rem, 6.2vw, 4rem);
    margin-top: 1.5rem;
  }
  .hero-lead {
    margin: 1.25rem auto 0;
    max-width: 44rem;
    text-wrap: pretty;
  }
  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.75rem;
    justify-content: center;
  }
  .hero-note {
    margin-top: 1rem;
    font-size: 0.8125rem;
    color: var(--muted-foreground);
  }
  .hero-shot {
    position: relative;
    margin-top: clamp(2.5rem, 6vw, 4rem);
  }

  /* ---- logo strip ---- */
  .strip {
    padding-block: 2.5rem;
    border-block: 1px solid var(--border);
    background: var(--surface);
  }
  .strip-label {
    text-align: center;
    font-size: 0.8125rem;
    color: var(--muted-foreground);
    margin-bottom: 1.25rem;
  }
  .strip-logos {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: 1.75rem;
    color: var(--muted-foreground);
  }
  .logo {
    display: grid;
    place-items: center;
    opacity: 0.65;
    transition:
      opacity 0.15s ease,
      color 0.15s ease;
  }
  .logo:hover {
    opacity: 1;
    color: var(--foreground);
  }

  /* ---- section furniture ---- */
  .section-alt {
    background: var(--surface);
    border-block: 1px solid var(--border);
  }
  .section-head {
    max-width: 44rem;
    margin-bottom: 3rem;
  }
  .section-head h2 {
    font-size: clamp(1.9rem, 4vw, 2.6rem);
    margin-top: 0.85rem;
  }
  .section-head .lead {
    margin-top: 1rem;
    max-width: 52ch;
  }

  /* ---- split (agent) ---- */
  .split {
    display: grid;
    gap: clamp(2rem, 5vw, 3.5rem);
    align-items: center;
  }
  @media (min-width: 1000px) {
    .split {
      grid-template-columns: 0.85fr 1.15fr;
    }
  }
  .stack {
    display: grid;
    gap: 1.75rem;
  }
  .point {
    display: flex;
    gap: 0.9rem;
  }
  .point-ico {
    display: grid;
    place-items: center;
    width: 2.25rem;
    height: 2.25rem;
    flex: none;
    border-radius: var(--radius);
    background: var(--primary-soft);
    color: var(--primary);
    border: 1px solid var(--primary-ring);
  }
  .point h3 {
    font-size: 1.0625rem;
    letter-spacing: -0.02em;
  }
  .point p {
    margin-top: 0.35rem;
    color: var(--muted-foreground);
    font-size: 0.9375rem;
  }

  /* ---- bento ---- */
  .bento {
    display: grid;
    gap: 1rem;
    margin-bottom: 1rem;
  }
  @media (min-width: 900px) {
    .bento {
      grid-template-columns: 1fr 1fr;
    }
    .bento-wide {
      grid-column: span 2;
    }
  }
  .bento-wide,
  .bento-half {
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }
  .bento-copy {
    padding: 1.75rem 1.75rem 0;
  }
  .bento-copy h3 {
    font-size: 1.1875rem;
  }
  .bento-copy p {
    margin-top: 0.5rem;
    color: var(--muted-foreground);
    font-size: 0.9375rem;
    max-width: 52ch;
  }
  .bento-shot {
    padding: 1.5rem 1.75rem 0;
    margin-top: auto;
  }
  /* Crop tall shots so the two half-cards stay the same height. */
  .bento-shot-crop :global(.shot) {
    max-height: 15rem;
    overflow: hidden;
  }

  /* ---- feature grid ---- */
  .feature-grid {
    display: grid;
    gap: 1.75rem 2.5rem;
    grid-template-columns: 1fr;
    margin-top: 3rem;
  }
  @media (min-width: 700px) {
    .feature-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
  @media (min-width: 1000px) {
    .feature-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  .feature-ico {
    display: grid;
    place-items: center;
    width: 2.25rem;
    height: 2.25rem;
    border-radius: var(--radius);
    background: var(--background);
    border: 1px solid var(--border);
    color: var(--foreground);
    margin-bottom: 0.9rem;
  }
  .feature h3 {
    font-size: 1rem;
  }
  .feature p {
    margin-top: 0.4rem;
    color: var(--muted-foreground);
    font-size: 0.9rem;
  }

  /* ---- self-host band ---- */
  .band {
    display: grid;
    gap: 2.5rem;
    align-items: center;
  }
  @media (min-width: 900px) {
    .band {
      grid-template-columns: 1.35fr 1fr;
    }
  }
  .band-copy h2 {
    font-size: clamp(1.8rem, 3.6vw, 2.4rem);
    margin-top: 0.85rem;
  }
  .band-copy .lead {
    margin-top: 1rem;
  }
  .install {
    margin-top: 1.5rem;
    padding: 1rem 1.15rem;
    background: var(--surface-2);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow-x: auto;
    font-size: 0.8125rem;
    line-height: 1.8;
  }
  .install code {
    white-space: pre;
    color: var(--foreground);
  }
  .install .c {
    color: var(--muted-foreground);
    user-select: none;
  }
  .band-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.9rem;
  }
  .band-list li {
    display: flex;
    align-items: flex-start;
    gap: 0.6rem;
    font-size: 0.9375rem;
    font-weight: 500;
  }
  .band-list :global(svg) {
    color: var(--primary);
    flex: none;
    margin-top: 0.2rem;
  }

  /* ---- integrations ---- */
  .integ-cols {
    display: grid;
    gap: 2.5rem;
    grid-template-columns: 1fr;
  }
  @media (min-width: 640px) {
    .integ-cols {
      grid-template-columns: 1fr 1fr;
    }
  }
  @media (min-width: 1000px) {
    .integ-cols {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  .integ-h {
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--muted-foreground);
    font-weight: 600;
    margin-bottom: 1rem;
  }
  .integ-col ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.85rem;
  }
  .integ-col li {
    display: flex;
    align-items: center;
    gap: 0.65rem;
    font-size: 0.9375rem;
    font-weight: 500;
  }
  .integ-logo {
    display: grid;
    place-items: center;
    width: 1.875rem;
    height: 1.875rem;
    flex: none;
    border-radius: 0.5rem;
    background: var(--background);
    border: 1px solid var(--border);
  }
  .integ-generic {
    color: var(--muted-foreground);
  }

  /* ---- pricing ---- */
  .notice {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 1.25rem;
    padding: 0.4rem 0.9rem;
    border: 1px solid var(--border);
    background: var(--background);
    color: var(--muted-foreground);
    border-radius: 999px;
    font-size: 0.8125rem;
  }
  .price-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: 1fr;
  }
  @media (min-width: 900px) {
    .price-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  .plan {
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    position: relative;
  }
  .plan.featured {
    border-color: var(--foreground);
    box-shadow: var(--shadow-md);
  }
  .plan-badge {
    position: absolute;
    top: -0.6875rem;
    right: 1.25rem;
    background: var(--action);
    color: var(--action-foreground);
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    padding: 0.2rem 0.6rem;
    border-radius: 999px;
  }
  .plan-name {
    font-size: 1rem;
  }
  .plan-price {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin-top: 0.5rem;
  }
  .plan-price .amount {
    font-size: 2.5rem;
    font-weight: 700;
    letter-spacing: -0.045em;
  }
  .plan-price .unit {
    color: var(--muted-foreground);
    font-size: 0.8125rem;
  }
  .plan-tagline {
    color: var(--muted-foreground);
    font-size: 0.9rem;
    margin-top: 0.5rem;
  }
  .plan-cta {
    width: 100%;
    margin-top: 1.35rem;
  }
  .plan-features {
    list-style: none;
    margin: 1.5rem 0 0;
    padding: 0;
    display: grid;
    gap: 0.7rem;
  }
  .plan-features li {
    display: flex;
    align-items: flex-start;
    gap: 0.55rem;
    font-size: 0.9rem;
    color: var(--muted-foreground);
  }
  .plan-features :global(svg) {
    color: var(--primary);
    flex: none;
    margin-top: 0.28rem;
  }
  .price-foot {
    text-align: center;
    color: var(--muted-foreground);
    font-size: 0.875rem;
    max-width: 62ch;
    margin: 2.5rem auto 0;
  }
  .price-foot strong {
    color: var(--foreground);
    font-weight: 600;
  }

  /* ---- FAQ ---- */
  .faq-list {
    display: grid;
    gap: 0;
    max-width: 46rem;
    border-top: 1px solid var(--border);
  }
  .faq {
    border-bottom: 1px solid var(--border);
  }
  .faq summary {
    list-style: none;
    cursor: pointer;
    padding: 1.25rem 0;
    font-weight: 600;
    font-size: 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
  }
  .faq summary::-webkit-details-marker {
    display: none;
  }
  .faq summary::after {
    content: '+';
    color: var(--muted-foreground);
    font-size: 1.25rem;
    line-height: 1;
    flex: none;
  }
  .faq[open] summary::after {
    content: '–';
  }
  .faq p {
    padding: 0 0 1.35rem;
    color: var(--muted-foreground);
    font-size: 0.9375rem;
    max-width: 68ch;
  }

  /* ---- final CTA ---- */
  .cta {
    text-align: center;
    padding: clamp(3rem, 7vw, 4.5rem) 1.5rem;
    border: 1px solid var(--border);
    border-radius: calc(var(--radius) + 0.5rem);
    background:
      radial-gradient(60% 120% at 50% 0%, var(--primary-soft), transparent 70%), var(--surface);
  }
  .cta h2 {
    font-size: clamp(2rem, 4.4vw, 2.75rem);
  }
  .cta .lead {
    margin: 1rem auto 0;
  }

  /* ---- footer ---- */
  .footer {
    border-top: 1px solid var(--border);
    padding-block: 3.5rem 2rem;
  }
  .footer-inner {
    display: grid;
    gap: 2.5rem;
    grid-template-columns: 1fr;
  }
  @media (min-width: 720px) {
    .footer-inner {
      grid-template-columns: 2fr 1fr 1fr 1fr;
    }
  }
  .footer-brand p {
    color: var(--muted-foreground);
    font-size: 0.875rem;
    margin-top: 0.75rem;
    max-width: 28ch;
  }
  .footer-col {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    font-size: 0.875rem;
    color: var(--muted-foreground);
  }
  .footer-col a:hover {
    color: var(--foreground);
  }
  .footer-h {
    color: var(--foreground);
    font-weight: 600;
    margin-bottom: 0.15rem;
  }
  .footer-base {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border);
    color: var(--muted-foreground);
    font-size: 0.8125rem;
  }
</style>
