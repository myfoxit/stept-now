import { defineConfig } from 'astro/config'

// Static marketing site. Set `site` to the real host so canonical/OG URLs and
// the sitemap resolve; the app itself lives at app.stepped.ai.
export default defineConfig({
  site: 'https://stepped.ai',
})
