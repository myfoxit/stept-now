# WXT's generated types + entrypoint scaffolding. Regenerated by `wxt prepare`
# (which runs on postinstall), so it must never be committed — a stale copy
# would shadow the real config and break `tsc`.
.wxt/
# `wxt zip` output
*.zip
