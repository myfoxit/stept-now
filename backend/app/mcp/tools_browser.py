"""Remote browser-drive MCP tools (Chrome extension over the WS gateway).

FILLED BY WAVE AGENT BE-B — tool list in docs/MCP-CONTRACTS.md. Register on
``app.mcp.server.mcp`` via ``@mcp.tool()``; delegate to
``app.services.remote_drive``; every tool requires TOURS_MANAGE.
"""

from __future__ import annotations
