"""Knowledge / RAG / articles / tours / conversations MCP tools.

FILLED BY WAVE AGENT BE-A1 — tool table in docs/MCP-CONTRACTS.md. Register on
``app.mcp.server.mcp`` via ``@mcp.tool()``; resolve auth per call through
``app.mcp.auth``.
"""

from __future__ import annotations
