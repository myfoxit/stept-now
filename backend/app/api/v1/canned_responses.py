"""canned_responses API — implemented by its wave agent (see docs/CONTRACTS.md).

The empty router is pre-registered; add routes here, never touch the registry.
"""

from fastapi import APIRouter

router = APIRouter()
