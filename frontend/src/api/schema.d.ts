/**
 * Generated OpenAPI types — run `make types` to regenerate from the backend.
 * This placeholder keeps tsc green until the first generation.
 */
export type paths = Record<string, never>
export type components = { schemas: Record<string, never> }
