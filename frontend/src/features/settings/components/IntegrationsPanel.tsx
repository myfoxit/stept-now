// Integrations catalog panel (FE-D fills; see docs/INTEGRATIONS-CONTRACTS.md).
// Card grid by category, OAuth connect flows, per-connection state, and the
// "use your own app" credential forms with the copyable redirect URI.

export function IntegrationsPanel() {
  return (
    <div className="p-6 text-sm text-muted-foreground">
      Integrations are being set up — check back shortly.
    </div>
  )
}
